sudo: required
language: node_js
node_js:
  - 'node'
  - 8
services:
  - mongodb
  - elasticsearch
notifications:
  email: true
cache: yarn
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.12.3
before_script:
  - sleep 10
after_success: yarn coverage
